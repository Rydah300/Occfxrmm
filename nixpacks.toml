[phases.setup]
nixPkgs = ["php83", "php83Extensions.pdo_pgsql", "php83Extensions.curl"]

[phases.install]
cmds = ["php -r \"copy('https://getcomposer.org/installer', 'composer-setup.php');\""]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
